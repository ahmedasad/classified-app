<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tool="http://schemas.android.com/tools">
    <data>
        <variable
            name="model"
            type="com.example.classifiedapp.model.getclassifiedlistresponse.Items" />
    </data>

    <androidx.cardview.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="@dimen/item_height"
        app:cardCornerRadius="@dimen/padding_medium"
        app:cardElevation="@dimen/padding_medium"
        android:layout_margin="@dimen/padding_tiny">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent">


            <ImageView
                android:id="@+id/img"
                android:layout_width="match_parent"
                android:layout_height="@dimen/item_img_height"
                android:scaleType="centerCrop"
                app:load_img="@{model.image_urls_thumbnails!=null? model.image_urls_thumbnails.empty?@string/app_name:model.image_urls_thumbnails.get(0):@string/app_name}"
                android:src="@color/cardview_dark_background"
                app:layout_constraintTop_toTopOf="parent" />

            <TextView
                android:id="@+id/title_text"
                style="@style/Animation.Design.BottomSheetDialog"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginHorizontal="@dimen/padding_tiny"
                android:maxLines="1"
                tool:text="dasdasdasdasdasdasdasdasdasdasdasdasdasdas"
                android:text="@{model.name}"
                android:ellipsize="end"
                android:layout_marginTop="@dimen/padding_micro"
                android:textStyle="bold"
                android:textSize="@dimen/text_medium"
                android:textColor="#575151"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toBottomOf="@id/img" />

            <TextView
                android:id="@+id/price_text"
                android:layout_width="match_parent"
                android:layout_height="0dp"
                app:layout_constraintBottom_toBottomOf="parent"
                android:gravity="end"
                app:layout_constraintVertical_bias="0"
                tool:text="AED 400 \n per item"
                app:set_price="@{model.price}"
                android:textColor="@color/black"
                android:textSize="@dimen/text_large"
                android:minLines="2"
                android:layout_marginHorizontal="@dimen/padding_tiny"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toBottomOf="@id/title_text" />


        </androidx.constraintlayout.widget.ConstraintLayout>
    </androidx.cardview.widget.CardView>
</layout>
